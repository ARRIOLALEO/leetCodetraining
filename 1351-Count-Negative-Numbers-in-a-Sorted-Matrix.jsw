/**
 * @param {number[][]} grid
 * @return {number}
 */
var countNegatives = function(grid) {
    let solution = 0
    grid.flat(Infinity).forEach(number =>{
        if(number < 0 ){
            solution++
        }
    })
    return solution
};
